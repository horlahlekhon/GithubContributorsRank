github-ranks {
  routes {
    # If ask takes more time than this to complete the request is failed
    ask-timeout = 100s
  }
}
akka.http.host-connection-pool.response-entity-subscription-timeout = 1.seconds